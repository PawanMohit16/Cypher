Run the API server

1. Create a .env file in the project root with:

PINATA_API_KEY=...
PINATA_SECRET_API_KEY=...

2. Start the server:

npm run dev:server

The server listens on http://localhost:4000 and exposes POST /api/pinJSON




